
//Set these variables depending on your environment
export namespace Config{
    export const SCHEME = 'http';
    export const HOSTNAME = '127.0.0.1';
    export const PORT = 8000;
    export const HOME_URL = SCHEME+'://'+HOSTNAME+':'+PORT;
}